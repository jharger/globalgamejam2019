import * as React from 'react';
import InventoryContainer from './InventoryContainer';
import { StyledRoot } from './styled/layout';

const Layout = () => (
  <StyledRoot>
    <InventoryContainer />
  </StyledRoot>
);

export default Layout;
